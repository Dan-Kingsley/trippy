<% content_for :title, "Trippy" %>

<section class="mb-10">
  <% if @trips.any? %>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">
      <% @trips.each do |trip| %>
        <%= render "trips/card", trip: trip %>
      <% end %>
    </div>
  <% else %>
    <p class="text-stone-500 dark:text-stone-400">No trips yet.</p>
  <% end %>
</section>
