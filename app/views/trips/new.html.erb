<% content_for :title, "#{t("trips.new.title")} - Trippy" %>

<h1 class="font-bold text-3xl mb-6 text-stone-800 dark:text-stone-100"><%= t("trips.new.heading") %></h1>

<%= render "form", trip: @trip, url: trips_path %>
